<?php

declare(strict_types=1);

use Illuminate\Support\Facades\Route;

// Note : la route racine `/` vit dans routes/tenant.php (groupe tenant + InitialiseTenant).
// La route `/control` vit dans routes/tenant.php avec prevent.tenant.access.
